import XCTest
@testable import TestbenchCoordinator

final class TestbenchCoordinatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TestbenchCoordinator().text, "Hello, World!")
    }
}
